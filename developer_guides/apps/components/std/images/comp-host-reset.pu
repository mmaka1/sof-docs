participant "audio/host" as host
participant "drivers/dma" as dma

autoactivate on
-> host : reset()

   host -> dma : dma_stop(dma, chan)
   host <-- dma

   host -> dma : dma_channel_put(dma, chan)
   host <-- dma
<-- host
